//import 'generateRandomNumberInRange' and execute

import generateRandomNumberInRange from "./generateRandomNumberInRange.js";

console.log(
  "The generated random number within the range is: " +
    generateRandomNumberInRange(100),
);
